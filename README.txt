git clone https://github.com/commanderx16/x16-emulator
into this directory. This will populate the x16-emulator directory with the source
files needed to build the emulator. The VS2019 project should already be setup
to build all the .c files and link them appropriately.